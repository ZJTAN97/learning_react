## Key learnings from the Article

- The initial value of a useState hook is always discarded on re-renders - it only has an effect when the component mounts.
- you can put a key attribute on any component to tell React: "Please mount this whenever the key changes. As long as the key is the same, please re-render".

## Example to enforce learning
